server:
  port: 8081

spring:
  application:
    name: ecommerce-app
  datasource:
    url: jdbc:postgresql://localhost:5432/ecommerce_db
    username: postgres
    password: changeme
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

jwt:
  secret: changeme-secret-key
  expiration-ms: 86400000

stripe:
  secret-key: changeme-stripe-key