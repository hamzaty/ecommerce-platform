server:
  port: 8081

spring:
  application:
    name: ecommerce-app
  datasource:
    url: jdbc:postgresql://localhost:5432/ecommerce_db
    username: postgres
    password: admin
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

jwt:
  secret: ceci-est-une-cle-secrete-beaucoup-plus-longue-pour-jwt-hs256-256bits
  expiration-ms: 86400000

stripe:
  secret-key: changeme-stripe-key